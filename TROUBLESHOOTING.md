# 문제 해결

## 원본 SHA-256 불일치

지원 대상과 다른 덤프이거나 파일명이 바뀐 경우입니다. 원본 파일을 다시 준비하고 README의 6개 SHA-256과 비교하세요. 이미 패치된 이미지, CHD에서 다시 변환한 이미지, 다른 리비전은 사용할 수 없습니다.

## 원본 파일이 없습니다

6개 BIN/CUE를 모두 같은 원본 폴더에 두고 파일명을 정확히 유지하세요. `-SourceDir`에는 파일이 아니라 폴더를 지정해야 합니다.

## xdelta3를 찾을 수 없습니다

`-Xdelta3`에 실행 파일 전체 경로를 지정하세요.

```powershell
-Xdelta3 'C:\Tools\xdelta3.exe'
```

## 출력 폴더가 비어 있지 않습니다

적용 실패로 생긴 파일이 남아 있거나 기존 게임 폴더를 출력으로 지정한 경우입니다. 새 빈 폴더를 사용하세요. 원본 폴더와 출력 폴더를 같게 지정할 수 없습니다.

## 스크립트 실행이 차단됩니다

릴리스 폴더에서 다음 명령으로 한 번만 우회 실행하세요.

```powershell
powershell -ExecutionPolicy Bypass -File .\apply_patch.ps1 -SourceDir '원본 폴더' -OutputDir '출력 폴더' -Xdelta3 'xdelta3.exe 경로'
```

## CD 음악이 들리지 않습니다

Track 1 BIN이 아니라 CUE를 불러왔는지 확인하세요. 출력 폴더의 Track 2 BIN을 이동하거나 이름을 바꾸면 안 됩니다.

## 디스크 2로 넘어가지 않습니다

게임이 교체를 요청한 상태에서 에뮬레이터의 디스크 교체 기능을 사용해 디스크 2 CUE를 선택하세요. 새 게임으로 다시 부팅하거나 데이터 트랙 BIN을 직접 실행하지 마세요.

## 글자가 깨지거나 일본어판 화면이 나옵니다

패치 스크립트 마지막에 PASS가 나왔는지 확인하고 출력 폴더의 CUE를 다시 선택하세요. 원본 CUE와 한글판 CUE를 혼동하기 쉬우므로 에뮬레이터의 최근 실행 목록도 확인하세요.

## 기존 세이브 사용

기존 일본판 또는 이전 개발 빌드의 세이브 호환은 보장하지 않습니다. v1.0.0에서 새로 시작해 저장하는 것을 권장합니다.

## 버그 제보

다음 정보를 이슈에 적어 주세요.

- 패치 버전
- 사용한 에뮬레이터와 버전
- 디스크 번호와 게임 진행 위치
- 재현 순서
- 가능하면 문제 화면 스크린샷

BIN/CUE, 패치가 적용된 전체 디스크 이미지, BIOS, 저작권 자료는 절대 첨부하지 마세요.
